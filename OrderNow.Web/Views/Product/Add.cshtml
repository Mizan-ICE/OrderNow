@model OrderNow.Web.Models.ViewModel.ProductVM
@{
    ViewData["Title"] = "Add";
}

<div class="container">
    <h1>Add New Product</h1>
    <form asp-action="Add" asp-controller="Product" method="post">
        <div class="form-group">
            <label asp-for="ProductName">Product Name:</label>
            <input asp-for="ProductName" class="form-control" />
            <span asp-validation-for="ProductName" class="text-danger"></span>
        </div>
        <div class="form-group">
            <label asp-for="ProductDescription">Product Description:</label>
            <textarea asp-for="ProductDescription" class="form-control"></textarea>
        </div>
        <div class="form-group">
            <label asp-for="Price">Price:</label>
            <input asp-for="Price" type="number" step="0.01" class="form-control" />
            <span asp-validation-for="Price" class="text-danger"></span>
        </div>
        <div class="form-group">
            <label asp-for="ProductImageUrl">Product Image Url (Optional):</label>
            <input asp-for="ProductImageUrl" class="form-control" />
            <span asp-validation-for="ProductImageUrl" class="text-danger"></span>
        </div>

        <div class="d-flex justify-content-between align-items-center my-4">
            <div class="form-group">
                <label asp-for="CategoryId">Category:</label>
                <select asp-for="CategoryId" class="form-control">
                    @foreach (var category in Model.Categories)
                    {
                        <option value="@category.Id">@category.CategoryName</option>
                    }
                </select>
            </div>
            <a asp-controller="Category" asp-action="NewCategory" class="btn btn-primary btn-green">New Category</a>
        </div>
        <button type="submit" value="Add" class="btn btn-primary">Create Product</button>
    </form>
</div>
